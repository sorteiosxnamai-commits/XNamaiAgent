[pytest]
pythonpath = .
asyncio_mode = strict
markers =
    offline_eval: honest offline agent replay / contract evals (no live OpenAI)
    online_eval: optional live Responses API evaluations
    integration: integration tests
    cross_repo: contract checks that need the sibling official repos checked out next to this one
